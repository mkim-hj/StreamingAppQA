Mirrors [Parse](https://github.com/ParsePlatform/Parse-SDK-iOS-OSX) using [Rome](https://github.com/neonichu/Rome) to create a Carthage-compatible Xcode project.

### To install:

- Add `github "Automatic/Parse"` to your `Cartfile`.
